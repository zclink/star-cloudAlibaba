
server:
  port: 8904

spring:
  application:
    name: shardingproxy-xa

mybatis:
  demo:
    configuration:
      cache-enabled: false
      local-cache-scope: statement
      log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
      map-underscore-to-camel-case: true
    type-aliases-package: com.gupaoedu.sca.dao



